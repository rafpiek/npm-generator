import appPlaceholder from './constants/AppPlaceholder.constants';
import regex from './constants/regex';
import inputTypes from './constants/inputTypes';
import apiRoutes from './constants/apiRoutes';

export default {
  APP_NAME: 'Todoardo',
  appPlaceholder,
  regex,
  inputTypes,
  apiRoutes
};
